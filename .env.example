# =============================================================================
# ENVIRONMENT CONFIGURATION
# =============================================================================
# 
# Copy this file to .env.local and fill in the values.
# 
# For production, set these as environment variables in Railway.
# 

# =============================================================================
# NEXTAUTH CONFIGURATION
# =============================================================================

# Required: Secret key for encrypting session tokens
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-auth-secret-here

# Required: URL of your application
# For development: http://localhost:3001
# For production: https://your-app.railway.app
NEXTAUTH_URL=http://localhost:3001

# =============================================================================
# CRON CONFIGURATION
# =============================================================================

# Required for production: Secret token for cron job authentication
# Generate with: openssl rand -hex 32
CRON_SECRET=your-cron-secret-here

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Port for the Next.js server
PORT=3001

# Optional: Path to SQLite database (defaults to ./collection_data.db)
# For Railway with persistent volume: /app/data/collection_data.db
# DATABASE_PATH=./collection_data.db

# Optional: Enable debug logging
# DEBUG=true
